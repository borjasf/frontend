/* Componente React: panel de filtros de búsqueda.
   Props: categorias[], filtrosActivos, onFiltroChange(filtros)
   Renderiza selects/inputs para: categoría, texto libre, estado (nuevo/usado) y precio máximo.
   Al cambiar cualquier filtro llama a onFiltroChange para que el padre (main.js) actualice la búsqueda. */
